const USERS_URL = '/api/user'; // Make sure this matches your backend route prefix

const TASKS_URL = "/api/task"; // Add /api prefix to match backend routes

const ADMIN_URL = "/api/admin";

export { USERS_URL, TASKS_URL, ADMIN_URL };
